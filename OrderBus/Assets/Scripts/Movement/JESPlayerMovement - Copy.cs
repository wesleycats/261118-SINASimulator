using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JESPlayerMovementCopy : MonoBehaviour {

	/*
	[SerializeField]
	private JESPlayerInput playerInput;
	[SerializeField]
	private float speed;

	private void Awake()
	{
		playerInput.OnInput += Move;
	}

	private void Move(InputActions inputAction, float axisAmount)	{
		if (inputAction == InputActions.Down || inputAction == InputActions.Up) {
			this.transform.position += new Vector3(0,axisAmount * Time.deltaTime * speed);
		} else if (inputAction == InputActions.Left || inputAction == InputActions.Right) {
			this.transform.position += new Vector3(axisAmount * Time.deltaTime * speed,0);
		}
	}

	private void Update()
	{
	
	}*/
}
